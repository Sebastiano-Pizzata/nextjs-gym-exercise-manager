import type { Metadata } from "next";
import "./globals.css";
import Providers from "./providers";
import Image from "next/image";
import ScrollTop from "../components/ScrollTop";


import { config } from "@fortawesome/fontawesome-svg-core";
import "@fortawesome/fontawesome-svg-core/styles.css";
config.autoAddCss = false;

export const metadata: Metadata = {
  title: "Gym Exercise Manager",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <Providers>
          <ScrollTop />
          <div
            className="relative min-h-screen w-full bg-cover bg-center"
            style={{
              backgroundImage: "url('/bg-palestra-2.jpg')",
            }}
          >
            <div className="absolute inset-0 bg-black/50"></div>
            <div className="relative z-10">
              <nav className=" bg-white/10 backdrop-blur-xl text-gray-900 rounded-3xl w-[90%] mx-auto px-4 py-2 shadow-xl shadow-black/10 transition-all duration-300">
                <div className="max-w-6xl mx-auto flex items-center justify-between flex-wrap gap-6">
                  <div className="">
                    <Image src="/gym-logo.svg" width={80} height={80} className="cursor-pointer" alt="Logo"></Image>
                  </div>
                  <div className="w-full max-w-md order-3 md:order-none">
                    {/* qua ci va la searchBar completa */}
                  </div>
                  <div className="flex-shrink-0">
                    <button className="px-6 py-3 bg-white/30 hover:bg-white/40 text-gray-900 border border-white/30 rounded-2xl text-sm font-semibold shadow-sm hover:shadow-md transition-all duration-300 cursor-pointer">
                      Crea Scheda
                    </button>
                  </div>
                </div>
              </nav>
              {children}
            </div>
          </div>
          <div>
            <footer className="bg-neutral-900 py-6 flex items-center justify-center">
              <p className="text-lg sm:text-xl font-medium text-gray-300 tracking-wide">
                &copy; GYM EXERCISE MANAGER. All rights reserved.
              </p>
            </footer>
          </div>
        </Providers>
      </body>



    </html >
  );
}
